# evals

`evals` is a hidden compatibility shortcut.
Do not advertise it in `help`, startup menus, or named next-step handoffs.

If the founder explicitly invokes it:

- Route to `experiment`.
- In that redirect, force the founder to define real proof of value, failure thresholds, and the difference between demo metrics and business metrics.
- If the founder is talking about production trust checks, mention that `trust` may also be needed after the experiment is framed.

Return a short redirect, not a fake eval framework.
